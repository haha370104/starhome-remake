# P4 装置能力实施台账

日期：2026-09-18。执行顺序继续 P0 → P1 → P2 → P3 → P4 → P6 → P5。
P3 的五行能量石范围尚待确认；P4 与其无强依赖，先独立接装置，不宣称 P3 全部完成。

## 原版证据与实施边界

| 分支 | 原客户端明确 | 复刻策略/未验证 |
| --- | --- | --- |
| 高热/腐蚀/磁化及赠品 | `cltobj/appendequipcltclass.fcc:1637–1861` 默认级别1/2/3，各自六档表；原说明为能量炮附加效果。高热默认3%、5秒、17/秒；腐蚀4%、4秒、降防35%；磁化5%、5秒、降攻28%、冷却3秒 | 本期先启用默认级别，等级加工另列；每次有效能量炮命中独立抽一次 |
| 新发生器 | `globalfunclt.fcc:2851–3794` 和 `great/wk_great.fcc:847–889,1444–1483` 给出基础生命、防御、炮攻击、概率、时间和状态值 | 原服务器触发与叠加算法不可见；品质0的基础能力先接，清除负面状态、品质成长、紫电特殊技能另列 |
| 烈焰 | `appendequipcltclass.fcc:5557` 基础炮攻击+10；`yl/mainclient_me_yl.fcc:623` 计入真实炮攻击 | 品质/阶数加值不冒充普通品质或接合器等级，不在基础能力阶段偷偷套用 |
| 隐身 | `dzl/appendequip/appendequipclt.fcc:199` 隐身目标令新导弹自爆；`bullet.fcc:456,486,599` 包含NPC导弹回调 | 普通怪物索敌是否受影响未证实；必须进一步确认当前怪物对应的导弹类型，不能把所有远程弹体当导弹 |
| 雷达 | `bullet.fcc:360–362` 玩家和陷阱两个分支；`npcclt1.fcc:4612` TrapBase 支持 RadarTrapHit | 关卡陷阱尚未投放；不能给普通怪物凭空增加隐身用途 |

26款目录的来源与全部数值见 `data/gameplay/generator_rules_v1.json`，可运行
`python -X utf8 tools/build_generator_rules.py --check` 复核。旧版数组的默认级别是源字段
`m_nGrade`，不能拿接合器的 `upgrade_level` 充当发生器级别。

新发生器中的“能量炮攻击压制”保留目标武器类型约束。当前未确认怪物的能量炮身份，
该分项先明确显示为未生效，不擅自降低其近战、毒雾、导弹等所有伤害。

## 明确采用的复刻规则

- 最多两件，各自独立触发。仅非致死的有效能量炮命中判定；未命中、拒绝开火、导弹、火箭不触发。
- 抽中且资源足够才扣1发发生器弹药及其配置能量，冷却从触发时刻开始。主炮已开火不会因装置缺弹失败。
- 同类持续伤害、百分比减防和降攻各取最强；不同来源可保留，强效果结束后较弱效果仍按原到期时间存在。
- 固定减防后计算百分比减防，结果最低0。高热每秒一次，包含结束的最后一秒，穿透防御。
- 连续命中只延长到期时间，不重置下次高热时钟；每个目标从首次中招起独立计时。
- 伤害归属实际提供最强高热的玩家，死亡仍走现有经验/任务/掉落与奖励切面，不新增另一套发奖逻辑。
- 来源卸装、死亡、切图、断开以及目标死亡清理状态；临时战斗效果不写入长期存档。

## 当前进度与验证

规则生成和独立领域模型完成，50项配置/叠加/错峰计时检查通过；尚待本阶段实际战斗、
弹药保存和UI接入，不能将此项当作P4完成。新测试在接通后纳入总门禁。

前置锻造/记忆模块阶段完整回归114/114通过，报告位于
`.godot/client-checks-20260918-025501-43072/summary.json`；该报告不覆盖随后新增P4运行代码。
剩余9项怪物命中特效继续遵循[独立审计](monster_hit_effect_audit.md)，没有证据时不填任意动画。
