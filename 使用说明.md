# starhome_remake 实现说明

本目录可直接由 Godot 4.7.2 打开，默认场景为 `scenes/main_hall.tscn`。

## 当前范围

- `assets/characters/`：玩家、非战斗 NPC 的裸模，以及生物共用人物阴影。
- `assets/monsters/`：奥姆虫、奥姆幼虫、感光质、毒胶四类怪物，共 62 组动画、1582 帧及 12 份外置调色板；目录按物种、变体、动作和共用特效组织。
- `assets/equipment/`：人物衣服与未来战车；当前玩家金色套装和 NPC 制服均与裸模分层存放、运行时同步叠加。
- `assets/items/`：背包/武器栏使用的中小图，当前包含能量炮和导弹。
- `assets/ui/`：纯 HUD、按钮和快捷栏素材。
- `assets/maps/`：地图底图、独立前景物件、小地图、碰撞和摆放清单。地图属于运行数据，单列目录而不混进场景装备。
- `data/monster_animations.json`：从免费版 `npcclt1.fcc` 整理的怪物类别、变体、动作、阴影、子弹和死亡动画映射。

后续继续按需导入，不把原客户端约 70% 暂时用不到的素材放进项目。

## 大厅实现

- `navigation_grid.bin/json` 由原始 `roomsvr1/indexdata.bin` 的全部 9800 个单元生成。原引擎使用 35×280 的交错菱形网格，中心间距 48×12；坐标换算按 `nengine.dll` 的 `enginebktile.cpp` 复刻。
- 反汇编确认标志字节的 `0x80` 位表示可通行：`0xD0` 可走，`0x50`、`0x10`、`0x00` 不可走。四个 `0x10` 格与四块 `AddImg` 立牌锚点精确重合。
- 右键移动先用八向 A* 绕障碍，再通过视线检测将网格路径拉直。每条无遮挡线段只选择一次最接近的八向动画，人物仍沿目标精确角度连续位移；点击障碍或地图外时，会改走到离点击位置最近且与玩家连通的可行走网格点。
- 玩家速度为 203 像素/秒；八帧行走动画为 14 FPS。二者都按原原型的 1.4 倍调整，因此每轮动画仍约移动 116 像素，不会重新产生脚底打滑。
- 玩家与 NPC 是 `阴影 → 裸模 → 装备 → 名称` 四层角色节点，动作和方向逐帧同步。
- 地图底图只含固定地面。四块立牌以原 FCC 的脚底锚点作为 Y 排序坐标：人物脚底 Y 小于立牌时被遮挡，大于立牌时显示在前面。
- HUD 使用固定像素锚点。窗口从 1280×720 放大到 1600×900 时，顶部栏、小地图、快捷栏和底部栏尺寸不变，可见世界范围随窗口扩大。
- Godot 4.7 的嵌入式调试画面还有独立的编辑器预览尺寸模式。右上角显示 `1280×720` 的菜单必须选择 `Stretch to Fit`；`Fixed Size` 会故意在调试容器四周留下灰边，与游戏自身的窗口伸缩设置无关。

## NPC 系统

- 大厅 9 个 NPC 都由 `data/npcs/yian_harbor_hall_floor_1.json` 实例化，主场景不再硬编码每个 NPC。配置包含外观、出生脚底坐标、业务类型、移动速度，以及三角巡逻的三个顺序点。
- 每个巡逻点都有独立的随机停留秒数区间。NPC 按顺序绕三角形循环，边与边之间继续使用地图的八向 A* 和路径拉直，动画方向只在线段切换时改变。
- `scripts/npcs/npc_base.gd` 统一负责移动、停留、动画和交互暂停；`shop_npc.gd`、`quest_npc.gd` 分别覆盖默认按钮与业务处理入口。以后增加制造师、仓库管理员等类型时，新建子类并在 `MainHall._create_npc_for_kind()` 注册即可。
- 左键点击任意 NPC 都会打开它自己的配置弹窗；交互期间该 NPC 暂停，关闭后恢复巡逻。当前商店和任务按钮只完成了正确的子类路由，真实商品与任务系统后续接入。

## 怪物动画

每个语义动作目录包含可实例化的 `preview.tscn`、`animation_frames.tres`、`frames.png` 和 `import_metadata.json`；索引色动画还包含 `indexed_frames.png` 与 `embedded_palette.png`。导出器原有的时间戳、`pic/pic2` 层级和通用文件名已从运行目录移除，原逻辑路径只保留在 `source_logical_path` 溯源字段中。10 FPS 只是导出预览值，动作语义以 `data/monster_animations.json` 为准。

索引纹理与各物种 `palettes/` 下的 256×1 调色板必须使用最近邻采样，关闭有损压缩和 mipmap。
