# 运行与手工验证

更新：2026-09-14。新对话先读 [交接](./docs/project_handoff.md)。启动命令见 [README](./README.md)，工程结构与已知缺口见
[评审导读](./docs/review_guide.md)。本文只说明操作，不重复维护素材数量或旧阶段状态。

## 进入游戏

- 默认场景 `scenes/main_hall.tscn`，使用进程内权威服务器；独立服务器模式加 `--online`。
- 进入时显示加载画面，等待服务端恢复角色地图并提交场景，不应先显示大厅再突然跳图。
- 角色状态读取服务器开发文件仓储；首次没有对应记录时才使用服务端初始模板。
- 当前身份仍是按连接顺序分配的开发角色。两个客户端、不同存档路径和启动顺序会影响
  读到哪个角色，不能当作正式账号系统。

## 操作

- **右键**：移动；不可达目标选择同一可达区域内的附近落点。
- **NPC 左键**：打开游戏内交互窗；已登记商店支持权威买卖，材料任务/每日训练支持领取和交付。
- **生产设施左键**：裁缝、烹饪、提炼和兵工厂制造打开非模态面板；仅登记的配方可执行。
- **野外左键**：攻击；命中掉落物时请求拾取，命中矿源时请求采集。结果由服务端确认。
- **主槽工程臂**：采掘臂才能挖矿；采掘臂/维修臂点击空地静默无动作，HUD 显示各自图标。
- **开火时移动继续**，炮口可独立转向；客户端爆炸特效不是扣血证据。
- **Z**：请求自维修。恢复量/能耗来自当前底盘和有效装备，维修技能参与准入。
- HUD 按钮打开人物、背包、战车等窗口；技能等级窗为非模态窗口，不应阻断移动。
- 按地图绑定的传送点切图。动画使用来源帧序；as 系列与 jt 系列保留各自样式。

## 窗口与显示

HUD 吸顶/吸底且保持像素大小。放大窗口增加世界可视范围，不改变人物或物品的原生比例。
Godot 嵌入式运行窗口的 `Fixed Size` 会留下预览边；需要撑满时改为 `Stretch to Fit`。
怪物名称仅悬浮显示；血条、能量条、伤害飘字与装备悬浮以各自逆向文档为验收依据。

## 建议手工回归顺序

1. 首次进入与重启恢复：确认位置、衣服、战车、背包、技能经验一致，加载屏时序正确。
2. 大厅→城区→D04→相邻野外，再返回；同时检查小地图、出口方向、角色/战车切换。
3. 八向与任意角度移动、绕障碍、就近落点；在两处楼梯前后检查人体和翅膀遮挡。
4. 移动中开炮、空炮、怪物死亡、拾取及背包满；核对伤害/能量/经验只由服务端改变。
5. 穿脱衣服、战车换装；世界外观、面板、属性提示和战斗数值同步。
6. 采矿、Z 维修、移动/攻击中断；退出重启后核对最后已提交状态。
7. 双客户端同图与分图、断线重连、同时拾取/攻击同一目标；不要只测默认进程内模式。

## 问题记录

记录地图、坐标、时间、操作顺序、是否独立服务器、预期与实际结果。命中差异按
[战斗诊断](./docs/combat_diagnostics.md)保留完整 JSONL；独立联机需要客户端和服务器两侧日志。
贴图/遮挡问题同时保留截图与资源/地图标识，不能仅据截图改碰撞或权威伤害。

自动检查入口和缺失覆盖见[评审导读的验证部分](./docs/review_guide.md)。
